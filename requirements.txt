# Face images and display
Pillow
pygame

# Microphone and wake word
sounddevice
# openwakeword currently needs NumPy 1.x; NumPy 2.x can break it.
numpy<2
openwakeword

# Local Ollama HTTP calls
requests

# Optional local Kokoro neural text-to-speech
# v1 Kokoro models use the binary voices-v1.0.bin format.
kokoro-onnx>=0.5.0
soundfile

# Web search tool
ddgs

# Tests
pytest
